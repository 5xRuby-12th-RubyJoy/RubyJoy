<%= form_with(model: @store) do |f| %>
    <div>
        <%= f.label '商店名稱' %>
        <%= f.text_field :title %>
    </div>
    <div>
        <%= f.label '商店描述' %>
        <%= f.text_area :description %>
    </div>
  
<% end %>